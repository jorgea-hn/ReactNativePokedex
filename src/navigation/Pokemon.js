import { View,Text } from "react-native";

export default function Pokemon(props){

    const {navigation, route} = props
    return(
        <View>
            <Text>Pokemon</Text>
        </View>
    )
}